name: Hello World
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "A demo build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run Test Step
        run: echo "Running"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the App
        run: echo "Deploy the App"
      
  
