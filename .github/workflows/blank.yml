name: Hello World
on:
  push:
    branches:
      - main
      - development
      - feature/*
  pull_request:
    types: opened
    branches:
      - main
      - development
      - feature/*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "A demo build"
